var Layout = require('./Layout');
var React = require('react');

var TestEmail = (props) => {
    return (
        <Layout>
            <h3>Some test content</h3>
        </Layout>
    );
};

module.exports = TestEmail;